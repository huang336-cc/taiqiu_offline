package android.webkit;

/**
 * 仅用于本地编译的占位 stub。
 * 设备运行时的 Android 框架（API 26+）自带真实 android.webkit.RenderProcessGoneDetail，
 * 本文件不会被打进 dex（不传给 d8），只用于让 javac 通过符号解析。
 */
public class RenderProcessGoneDetail {
    public boolean didCrash() { return false; }
    public int getCrashReason() { return 0; }
}
