# 发布记录

## v1.0.3（当前可用版本）

发布时间：2026 年 8 月 3 日

### 修复内容

修复**开始游戏后界面全黑、没有任何 3D 场景**的问题。

**根因**：three.js 的 `FileLoader` 使用 `fetch()` 加载 `.gltf`、`.bin`、纹理和音效文件；Android WebView 在 `file://` 页面中直接拒绝任何 `file://` 请求的 fetch，错误为 `URL scheme file is not supported`。这导致桌台模型和贴图全部加载失败，3D 场景为空，玩家只看到黑色背景（而主菜单是内联 HTML，所以能正常显示）。

**解法**：不再使用 `file:///android_asset/`，改为让 WebView 加载虚拟 https 域名 `https://billiards.local/`，并通过 `WebViewClient.shouldInterceptRequest()` 在本地把所有请求映射回 APK 内的 `assets/dist`。页面 origin 变成正常的 https，fetch 完全可用；同时不创建任何真实网络连接，依旧零权限、纯离线。

**改动**：

- `MainActivity.java`：重写 `shouldInterceptRequest()`，实现本地资产拦截器；加载入口改为 `https://billiards.local/menu.html`
- `MainActivity.java`：继续强制硬件加速，并通过反射尝试开启 `setAllowFileAccessFromFileURLs` / `setAllowUniversalAccessFromFileURLs` / `setMediaPlaybackRequiresUserGesture`
- `webgl.ts`：新增诊断浮层。若 WebGL 上下文创建失败，会在屏幕上显示明确错误原因，避免纯黑屏
- `menu.html` / `menu-cn.js`：在「关于」页增加开发者 **huang336** 信息，并修复版本号重复显示
- `versionCode` `26080402` → `26080403`
- `versionName` `1.0.2` → `1.0.3`

### 线上地址

| | |
|---|---|
| Release 页 | <https://github.com/huang336-cc/taiqiu_offline/releases/tag/v1.0.3> |
| APK 直链 | <https://github.com/huang336-cc/taiqiu_offline/releases/download/v1.0.3/billiards-cn-v1.0.3.apk> |

### 上线后复核（匿名下载线上包实测）

| 检查项 | 结果 |
|---|---|
| 下载完整性 | SHA256 `b203b9274a470352781eb972b0bf00c1a4ff4f20ab16caa4ea661b9238ccbdeb`，与本地构建一致 |
| `resources.arsc` | **Stored（未压缩）** |
| 4 字节对齐 | `zipalign -c 4` 通过 |
| 签名 | v1 + v2 + v3 全部通过 |
| 权限数量 | 0 |
| 包名 / 版本 | `com.tailuge.billiards.cn` / 1.0.3 (26080403) |
| minSdk / targetSdk | 21 / 35 |
| 原生库 | 无（不受 16 KB 分页限制影响） |

### 版本对照

| 版本 | `resources.arsc` | 能否在 Android 11+ 安装 | 能否正常显示 3D 场景 |
|---|---|---|---|
| v1.0.0 | `Defl:N`（压缩） | 否 | 否 |
| v1.0.1 | `Defl:N`（压缩） | 否 | 否 |
| v1.0.2 | `Stored` | **是** | 否 |
| v1.0.3 | `Stored` | **是** | **是** |

---

## v1.0.2

发布时间：2026 年 8 月 3 日

### 修复内容

修复 **Android 11 及以上全系机型**的安装失败。v1.0.0 与 v1.0.1 均无法安装，已在 GitHub 上标注废弃。

**根因**：组包时 `zip -qr` 把 `resources.arsc` 一并压缩了。

自 Android 11（API 30）起，系统强制要求 APK 内的 `resources.arsc` 以 **STORED（不压缩）** 方式存放且 4 字节对齐，否则包管理器拒绝解析，返回 `INSTALL_PARSE_FAILED_RESOURCES_ARSC_COMPRESSED`，其数值正是 **-124**。小米澎湃 OS 把这类错误统一显示成「该安装包与您的系统不兼容」，极易被误判为系统版本问题。

`zipalign` 只负责对齐、不会解压，所以补跑对齐救不回来 —— 必须在组包阶段就把 `resources.arsc` 单独以 store 方式写入。

**改动**：

- 组包流程：其余文件正常压缩，`resources.arsc` 用 `zip -Z store` 单独追加
- `build-apk.sh` 增加出厂校验，`resources.arsc` 压缩方式或对齐不合格则构建直接失败并退出，杜绝再次分发坏包
- `build-apk.sh` 补上 d8 输出目录的 `mkdir`
- `versionCode` `26080401` → `26080402`

### 线上地址

| | |
|---|---|
| Release 页 | <https://github.com/huang336-cc/taiqiu_offline/releases/tag/v1.0.2> |
| APK 直链 | <https://github.com/huang336-cc/taiqiu_offline/releases/download/v1.0.2/billiards-cn-v1.0.2.apk> |

### 上线后复核（匿名下载线上包实测）

| 检查项 | 结果 |
|---|---|
| 下载完整性 | SHA256 `2866f1cf20a41fb0ffe706020eb5fac262dc75f27d3aa560a7f5f0b3ea874ca9`，与本地构建一致 |
| `resources.arsc` | **Stored（未压缩）** |
| 4 字节对齐 | `zipalign -c 4` 通过 |
| 签名 | v1 + v2 + v3 全部通过 |
| 权限数量 | 0 |
| 包名 / 版本 | `com.tailuge.billiards.cn` / 1.0.2 (26080402) |
| minSdk / targetSdk | 21 / 35 |
| 原生库 | 无（不受 16 KB 分页限制影响） |

### 版本对照

| 版本 | `resources.arsc` | 能否在 Android 11+ 安装 |
|---|---|---|
| v1.0.0 | `Defl:N`（压缩） | 否 |
| v1.0.1 | `Defl:N`（压缩） | 否 |
| v1.0.2 | `Stored` | **是** |

---

## v1.0.1（已废弃）

发布时间：2026 年 8 月 3 日

### 修复内容

> **本版未能解决问题，判断方向有误。** 当时把 `-124` 误判成版本降级/targetSdk 过旧，实际根因是 `resources.arsc` 被压缩，见 v1.0.2。

改动：

- `targetSdkVersion`：`34` → `35`
- `versionCode`：`26080303` → `26080401`
- `versionName`：`1.0.0` → `1.0.1`
- 功能、界面、零权限承诺保持不变

> **签名密钥已更换**：v1.0.1 起使用新的自签名密钥，与 v1.0.0 不互容。若设备上装过任何旧版，请先卸载再安装。v1.0.2 沿用 v1.0.1 的密钥。

### 线上地址

| | |
|---|---|
| Release 页 | <https://github.com/huang336-cc/taiqiu_offline/releases/tag/v1.0.1> |
| APK 直链 | <https://github.com/huang336-cc/taiqiu_offline/releases/download/v1.0.1/billiards-cn-v1.0.1.apk> |

### 校验

| 文件 | 大小 | SHA256 |
|---|---|---|
| `billiards-cn-v1.0.1.apk` | 590 KB | `07c8e5370bade365ddcc2f53bcd928e0d09051f1cc1ffb1e964923a4f4cdb13e` |

权限计数：**0 个** | 签名：v1 + v2 + v3 通过 | targetSdk：**35**

---

## v1.0.0（已废弃）

发布时间：2026 年 8 月 3 日

---

## 线上地址

| | |
|---|---|
| 仓库主页 | <https://github.com/huang336-cc/taiqiu_offline> |
| Release 页 | <https://github.com/huang336-cc/taiqiu_offline/releases/tag/v1.0.0> |
| APK 直链 | <https://github.com/huang336-cc/taiqiu_offline/releases/download/v1.0.0/billiards-cn-v1.0.0.apk> |
| 源码包直链 | <https://github.com/huang336-cc/taiqiu_offline/releases/download/v1.0.0/billiards-cn-source-v1.0.0.tar.gz> |

仓库为**公开**，协议被 GitHub 正确识别为 **GPL-3.0**。

> 附件名用了 ASCII。GitHub 会把 Release 资产名里的非 ASCII 字符逐个替换成点号，
> 中文文件名传上去会显示成一串点。手机上安装后的应用名仍然是「台球大师」。

---

## 发布内容

### 仓库源码（main 分支，205 个文件）

```
billiards-cn/          游戏本体（TypeScript 源码 + dist 构建产物）
billiards-apk/         安卓封装（清单 + MainActivity.java + 资源 + build-apk.sh）
docs/                  安装说明、发布说明
README.md              项目说明与引用声明
LICENSE                GPL-3.0 全文
开源声明.md             衍生声明与第三方组件清单
```

**未入库**：`node_modules/`、构建中间产物、以及签名私钥 `release.keystore`（已在 `.gitignore` 中拦截）。

### Release 附件

| 文件 | 大小 | SHA256 |
|---|---|---|
| `billiards-cn-v1.0.0.apk` | 590 KB | `149785677e5d36dd1c046d88f0659f47b8f0e4f0c82ffee8353608a8df5e9a3f` |
| `billiards-cn-source-v1.0.0.tar.gz` | 689 KB | `a03ee758d5d2c339a12eba477ad3167472836ab0b984e70bb62abd6dfaaa7287` |

---

## 上线后验证

以匿名身份（不带令牌）从公网重新下载两个附件，逐项复核：

| 检查项 | 结果 |
|---|---|
| APK 下载完整性 | SHA256 与本地一致 |
| 源码包下载完整性 | SHA256 与本地一致 |
| 线上 APK 权限数量 | **0 个**（`aapt2 dump badging` 无任何 `uses-permission`） |
| 线上 APK 签名 | v1 + v2 + v3 三套签名方案均校验通过 |
| 包名 / 版本 | `com.tailuge.billiards.cn` / 1.0.0 (26080303) |
| minSdk / targetSdk | 21 / 34 |
| 应用显示名 | 台球大师 |
| 仓库首页引用声明 | 匿名访问可见 tailuge/billiards、GPL-3.0 字样 |

---

## 引用与协议合规

Release 说明、仓库 README、`开源声明.md`、以及应用内的「开源许可」页面均已完整写明：

- 原始项目 [tailuge/billiards](https://github.com/tailuge/billiards)，作者 tailuge，协议 GPL-3.0，基线版本 0.3.1
- 依 GPL-3.0 §5(a) 的修改声明与修改日期（2026-08-03）
- 依 GPL-3.0 §6 的完整源码提供（Release 附件 + 仓库 main 分支双通道）
- 第三方组件：three.js 0.185.1、JSONCrush 1.1.8、interact.js 1.10.28（均为 MIT）
- 依 GPL-3.0 §15、§16 的无担保声明

---

## 后续维护

发新版本时改 `发布到GitHub.sh` 里的 `TAG` 与 `TITLE`，然后：

```bash
export GH_TOKEN='你的令牌'
bash 发布到GitHub.sh
```

重新构建 APK：

```bash
cd billiards-cn && node_modules/.bin/webpack
export ANDROID_SDK=/path/to/android-sdk
cd ../billiards-apk && ./build-apk.sh
```

---

## 安全提醒

本次发布使用的个人访问令牌 `ghp_ZCiY…f1pL` 曾以明文形式出现在对话中，且权限范围很大
（含 `repo`、`delete_repo`、`workflow`、`write:packages` 等）。
**建议尽快到 <https://github.com/settings/tokens> 撤销该令牌**，需要时另建一个仅含 `public_repo` 的细粒度令牌。

令牌全程只发往 `api.github.com` / `uploads.github.com` / `github.com` 官方域名，未经过任何第三方镜像代理。
